[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "golem-docs"
version = "0.1.0a2"
description = "A static site generator built on AsciiDoc, designed for highly technical interactive documentation."
readme = { file = "README.adoc", content-type = "text/plain" }
requires-python = ">=3.14"
license = { text = "Apache-2.0" }
authors = [
    { name = "Michael Bernstein" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
dependencies = [
    "asciidoctrine>=0.2.0a5",
    "asciidocstring>=0.1.0a8",
    "asciidoctest>=0.2.0a4",
    "asciidoctype>=0.1.0a5",
    "chameleon",
    "click",
    "filelock",
    "pluggy",
    "pygments>=2.18",
    "griffe>=1.5.0",
    "tomli; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/webmaven/golem"
Documentation = "https://webmaven.github.io/golem/"
Repository = "https://github.com/webmaven/golem.git"
Issues = "https://github.com/webmaven/golem/issues"

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "coverage[toml]",
    "ruff",
    "mypy",
    "requests",
]
docs = [
    "golem-docs",  # self-hosting: use golem itself to build golem's docs
]

[project.scripts]
golem = "golem.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/golem"]

[tool.golem.site]
title = "Golem Documentation"
author = "Michael Bernstein"

[tool.golem.build]
content_dir = "docs"
output_dir = "dist"
theme = "default"

[tool.golem.plugins]
plugins = [
    "golem.plugins.apidoc",
    "golem.plugins.doctest",
]

[tool.golem.api]
packages = ["golem"]
output_dir = "api"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--ignore=ARCHITECTURE.adoc --ignore=docs"

[tool.ruff]
line-length = 127
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "C90"]
ignore = ["C901", "E501"]

[tool.ruff.format]
quote-style = "double"

[tool.coverage.run]
source = ["src/golem"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false
fail_under = 70


